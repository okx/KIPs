name: web-nodejs

runtime: nodejs

vars:
  stages:
    local:
      apiBaseUrl: "https://dev.example.org/v1"
    dev:
      apiBaseUrl: "https://dev.example.org/v1"
    prod:
      apiBaseUrl: "https://prod.example.org/v1"

plugins:
  - squeezer-serve